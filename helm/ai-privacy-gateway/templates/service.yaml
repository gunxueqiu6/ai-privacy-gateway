apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-privacy-gateway.fullname" . }}
  labels:
    {{- include "ai-privacy-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-privacy-gateway.selectorLabels" . | nindent 4 }}