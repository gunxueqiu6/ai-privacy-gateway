name: Release AI Privacy Gateway

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

jobs:
  build-lite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/ai-privacy-gateway
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest
            type=raw,value=lite

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and load Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan Docker image with Trivy
        run: |
          docker run --rm aquasec/trivy image ghcr.io/${{ github.repository_owner }}/ai-privacy-gateway:lite --severity CRITICAL,HIGH --exit-code 0

      - name: Verify Docker image
        run: |
          docker run -d --name ci-test -p 19999:9999 ghcr.io/${{ github.repository_owner }}/ai-privacy-gateway:lite
          sleep 5
          curl -f --retry 3 --retry-delay 2 http://localhost:19999/health || (docker logs ci-test && exit 1)
          docker stop ci-test

      - name: Build and push Docker image with provenance
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          provenance: true
          sbom: true
          cache-from: type=gha

      - name: Export Docker image
        run: |
          docker save ghcr.io/${{ github.repository_owner }}/ai-privacy-gateway:lite > lite_image.tar

      - name: Generate Docker image checksum
        run: sha256sum lite_image.tar > lite_image.tar.sha256

      - name: Upload Docker checksum
        uses: actions/upload-artifact@v4
        with:
          name: lite-docker-checksum
          path: lite_image.tar.sha256

      - name: Upload Lite Docker image
        uses: actions/upload-artifact@v4
        with:
          name: lite-docker-image
          path: lite_image.tar

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Windows executable
        run: pyinstaller --onefile --name PrivacyGateway --add-data "static:static" --add-data "entity_catalog.json:." main.py

      - name: Generate Windows executable checksum
        run: certutil -hashfile dist/PrivacyGateway.exe SHA256 > PrivacyGateway.exe.sha256

      - name: Upload Windows checksum
        uses: actions/upload-artifact@v4
        with:
          name: windows-checksum
          path: PrivacyGateway.exe.sha256

      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist/PrivacyGateway.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build macOS executable
        run: pyinstaller --onefile --name PrivacyGateway --add-data "static:static" --add-data "entity_catalog.json:." main.py

      - name: Generate macOS executable checksum
        run: shasum -a 256 dist/PrivacyGateway > PrivacyGateway.sha256

      - name: Upload macOS checksum
        uses: actions/upload-artifact@v4
        with:
          name: macos-checksum
          path: PrivacyGateway.sha256

      - name: Upload macOS executable
        uses: actions/upload-artifact@v4
        with:
          name: macos-exe
          path: dist/PrivacyGateway

  publish-js-sdk:
    runs-on: ubuntu-latest
    env:
      # Secrets cannot be referenced in if: conditionals; pass through env
      NPM_PUBLISH_ENABLED: ${{ secrets.NPM_TOKEN != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: 'https://registry.npmjs.org'
          cache: npm
          cache-dependency-path: sdk/js/package-lock.json

      - name: Build JS SDK
        run: |
          npm ci
          npm run build
        working-directory: sdk/js

      - name: Verify version matches tag
        run: |
          PKG_VERSION=$(node -p "require('./sdk/js/package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "ERROR: package.json version ($PKG_VERSION) does not match tag ($TAG_VERSION)"
            exit 1
          fi
          echo "Version check passed: $PKG_VERSION"

      - name: Publish to npm
        # Skip when the repo has no NPM_TOKEN secret configured
        if: env.NPM_PUBLISH_ENABLED == 'true'
        run: npm publish
        working-directory: sdk/js
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  release:
    needs: [build-lite, build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Download only the release attachments individually (bulk download of
      # large artifacts can fail with "download failed after 5 retries").
      # The Docker image itself is published to GHCR — no need for the 72MB
      # tar in the GitHub Release.
      - name: Download Windows executable
        uses: actions/download-artifact@v5
        with:
          name: windows-exe
          path: release-assets/windows-exe

      - name: Download Windows checksum
        uses: actions/download-artifact@v5
        with:
          name: windows-checksum
          path: release-assets/windows-checksum

      - name: Download macOS executable
        uses: actions/download-artifact@v5
        with:
          name: macos-exe
          path: release-assets/macos-exe

      - name: Download macOS checksum
        uses: actions/download-artifact@v5
        with:
          name: macos-checksum
          path: release-assets/macos-checksum

      - name: Extract release notes from CHANGELOG
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          python .github/scripts/release_notes.py "$VERSION" > release_body.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_body.md
          files: |
            release-assets/windows-exe/PrivacyGateway.exe
            release-assets/windows-checksum/PrivacyGateway.exe.sha256
            release-assets/macos-exe/PrivacyGateway
            release-assets/macos-checksum/PrivacyGateway.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
