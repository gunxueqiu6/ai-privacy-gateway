name: privacy_gateway_sdk
description: Protect sensitive data (phone numbers, emails, IDs, bank cards) in Flutter AI app interactions through automatic PII masking and restoration.
version: 1.0.0
homepage: https://privacygw.pages.dev
repository: https://github.com/gunxueqiu6/ai-privacy-gateway
issue_tracker: https://github.com/gunxueqiu6/ai-privacy-gateway/issues
documentation: https://github.com/gunxueqiu6/ai-privacy-gateway/tree/master/sdk/flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  json_annotation: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

flutter:
  # No platform-specific assets

# For publishing to pub.dev
# Run: flutter pub publish