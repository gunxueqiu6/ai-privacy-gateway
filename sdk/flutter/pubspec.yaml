name: privacy_gateway_sdk
description: AI Privacy Gateway SDK for Flutter - Protect sensitive data in AI interactions
version: 1.0.0
homepage: https://github.com/gunxueqiu6/ai-privacy-gateway

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0

flutter:
  # No platform-specific assets

# For publishing to pub.dev
# Run: flutter pub publish