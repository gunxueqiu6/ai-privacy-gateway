---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="使用 Cursor AI 时如何保护隐私 — 阻止源代码和 PII 泄漏"
  description="了解如何阻止 Cursor AI 泄漏你的源代码、API 密钥和敏感数据。30 秒搭建 AI Privacy Gateway 作为本地代理，无需修改代码即可自动脱敏敏感信息。"
  keywords="Cursor隐私保护,Cursor代码上传安全,Cursor AI数据防护,IDE隐私代理,AI编辑器源代码保护"
  canonicalURL="https://privacygw.pages.dev/protect-cursor-privacy"
  ogType="article">
  <Nav />

  <section class="seo-page">
    <div class="container">
      <div class="seo-header">
        <div class="seo-badge">Cursor AI 隐私指南</div>
        <h1>使用 Cursor AI 时如何保护你的隐私</h1>
        <p class="seo-subtitle">
          Cursor 每次请求 AI 帮助时都会将你的代码发送到 OpenAI 和 Claude 等 AI API。
          没有隐私代理，代码片段、API 密钥和数据库凭证都可能离开你的机器。
          以下是阻止方法。
        </p>
      </div>

      <div class="seo-tldr glass">
        <strong>TL;DR：</strong>修改 Cursor API 配置中的一个设置。将 Base URL 设为
        <code>http://localhost:9999/v1</code>，在本地运行网关，所有敏感数据在离开你的机器前
        都会被自动脱敏。仅需 30 秒。
      </div>

      <h2>为什么 Cursor AI 是隐私风险</h2>
      <p>
        Cursor 是一个强大的 AI 辅助代码编辑器，但每次你使用其 AI 功能——行内编辑、聊天、
        代码生成或调试——你的代码都会被发送到第三方 AI API。这包括：
      </p>
      <ul>
        <li><strong>源代码</strong>来自当前文件和打开的标签页</li>
        <li><strong>API 密钥、令牌和机密</strong>恰好在你的代码中</li>
        <li><strong>数据库连接字符串</strong>包含凭证信息</li>
        <li><strong>内部业务逻辑</strong>和专有算法</li>
        <li><strong>客户数据</strong>在代码库中被引用的内容</li>
      </ul>
      <p>
        <a href="https://docs.cursor.com/privacy" rel="nofollow">Cursor 的隐私政策</a>
        声明他们默认不会用你的代码进行训练，但你的数据仍然会经过第三方 AI 提供商。
        本地隐私代理是确保敏感数据保持在你控制之下的唯一方式。
      </p>

      <h2>AI Privacy Gateway 如何保护 Cursor AI</h2>
      <p>
        AI Privacy Gateway 作为一个本地反向代理，位于 Cursor 和 AI API 之间。
        每个请求在转发前都会经过网关的敏感模式扫描：
      </p>

      <div class="code-compare glass">
        <div class="code-compare-header">保护 Cursor AI API 调用</div>
        <div class="flow-diagram">
          <div class="flow-node">Cursor</div>
          <span class="flow-arrow">&rarr;</span>
          <div class="flow-node flow-accent">隐私网关</div>
          <span class="flow-arrow">&rarr;</span>
          <div class="flow-node flow-danger">OpenAI / Claude API</div>
        </div>
        <div class="flow-note">网关在数据到达 AI API 前脱敏 API 密钥、机密、数据库凭证和 PII</div>
      </div>

      <h2>设置指南：30 秒保护 Cursor</h2>

      <h3>第 1 步：安装网关</h3>
      <p>选择你喜欢的方式：</p>

      <div class="code-block glass">
        <div class="code-block-title">Docker（推荐）</div>
        <pre><code>docker run -d \
  --name ai-privacy-gw \
  -p 9999:9999 \
  -v ./vault_data:/app/vault_data \
  ghcr.io/gunxueqiu6/ai-privacy-gateway:lite</code></pre>
      </div>

      <div class="code-block glass">
        <div class="code-block-title">Python（无需 Docker）</div>
        <pre><code>git clone https://github.com/gunxueqiu6/ai-privacy-gateway
cd ai-privacy-gateway
pip install -r requirements.txt
python main.py</code></pre>
      </div>

      <h3>第 2 步：配置 Cursor</h3>
      <p>
        在 Cursor 中，进入 <strong>设置 &rarr; API Key &rarr; Overwrite OpenAI Base URL</strong>
        并将其设置为：
      </p>
      <div class="code-block glass">
        <pre><code>http://localhost:9999/v1</code></pre>
      </div>
      <p>在 API Key 字段中保留你原有的 API 密钥。网关会自动转发。</p>

      <h3>第 3 步：验证</h3>
      <p>
        打开 Cursor 的 AI 聊天，发送一条包含 API 密钥或手机号等敏感数据的测试消息。
        检查网关管理面板 <a href="http://localhost:9999/admin">http://localhost:9999/admin</a>
        查看实时检测和脱敏的实体。
      </p>

      <h2>Cursor 用户正在保护的内容</h2>
      <p>使用 AI Privacy Gateway，Cursor 用户自动脱敏以下内容：</p>
      <div class="entity-grid">
        <div class="entity-item"><span class="entity-chip chip-secret">API 密钥</span> sk-*, AKIA*, gh* 令牌在离开前被脱敏</div>
        <div class="entity-item"><span class="entity-chip chip-phone">手机号</span> 代码注释中的用户电话号码</div>
        <div class="entity-item"><span class="entity-chip chip-email">邮箱</span> 测试邮箱、代码中的用户邮箱</div>
        <div class="entity-item"><span class="entity-chip chip-id">身份证</span> 测试数据中的身份证号</div>
        <div class="entity-item"><span class="entity-chip chip-bank">银行卡</span> 支付测试数据、信用卡号</div>
        <div class="entity-item"><span class="entity-chip chip-name">姓名</span> 代码中的用户名、员工名</div>
        <div class="entity-item"><span class="entity-chip chip-loc">地址</span> 数据样本中的客户地址</div>
        <div class="entity-item"><span class="entity-chip chip-org">机密</span> 数据库 URL、连接字符串、密码</div>
      </div>

      <h2>为什么不仅仅用 Cursor 的隐私模式？</h2>
      <p>
        Cursor 提供隐私模式来避免代码存储，但它并<strong>不能</strong>阻止你的代码被发送到 AI API 提供商。
        像 AI Privacy Gateway 这样的本地代理是确保敏感数据永远不以明文形式到达第三方服务器的
        <strong>唯一</strong>方式。
      </p>

      <!-- FAQ -->
      <div class="seo-faq glass" id="faq">
        <h2>常见问题</h2>

        <div class="faq-item">
          <h3>AI Privacy Gateway 会拖慢 Cursor 吗？</h3>
          <p>不会。网关平均每个请求增加不到 1ms 的延迟。P99 延迟保持在 2ms 以下。你不会注意到 Cursor 响应速度的任何变化。</p>
        </div>

        <div class="faq-item">
          <h3>能否与 Cursor 内置的 API 密钥一起使用？</h3>
          <p>可以。只需在 Cursor 的 API 配置中更改 base URL 设置。继续使用你现有的 API 密钥——网关会转发它。</p>
        </div>

        <div class="faq-item">
          <h3>这适用于 Cursor Tab 吗？</h3>
          <p>适用。所有经过 API 的 Cursor AI 功能——行内编辑、聊天、代码生成和调试——都会受到网关保护。</p>
        </div>

        <div class="faq-item">
          <h3>如果我使用自定义 API 端点呢？</h3>
          <p>AI Privacy Gateway 支持任何兼容 OpenAI 的 API。将 Cursor 的 base URL 设为 <code>http://localhost:9999/v1</code>，并配置网关的 <code>TARGET_LLM</code> 环境变量指向你的自定义端点。</p>
        </div>

        <div class="faq-item">
          <h3>它免费吗？</h3>
          <p>是的。AI Privacy Gateway 在 MIT 许可下开源。个人和商业使用均免费。无需账户或信用卡。</p>
        </div>

        <div class="faq-item">
          <h3>可以在团队中使用吗？</h3>
          <p>可以。Docker 镜像可以部署在共享服务器上，多个团队成员可以将他们的 Cursor 流量路由到该服务器。每个用户的保险库数据保持隔离。</p>
        </div>
      </div>

      <!-- CTA -->
      <div class="seo-cta">
        <h2>阻止通过 Cursor AI 泄漏代码</h2>
        <p>30 秒部署 AI Privacy Gateway。无需修改代码。</p>
        <div class="seo-cta-buttons">
          <a href="/download" class="btn btn-primary btn-lg">免费开始使用</a>
          <a href="/demo" class="btn btn-ghost btn-lg">试用演示</a>
        </div>
      </div>

      <!-- Breadcrumb list for this page -->
      <script type="application/ld+json">{
        "@context": "https://schema.org",
        "@type": "BreadcrumbList",
        "itemListElement": [
          { "@type": "ListItem", "position": 1, "name": "首页", "item": "https://privacygw.pages.dev" },
          { "@type": "ListItem", "position": 2, "name": "保护 Cursor 隐私", "item": "https://privacygw.pages.dev/protect-cursor-privacy" }
        ]
      }</script>

      <!-- FAQ Schema -->
      <script type="application/ld+json">{
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          { "@type": "Question", "name": "AI Privacy Gateway 会拖慢 Cursor 吗？", "acceptedAnswer": { "@type": "Answer", "text": "不会。网关平均每个请求增加不到 1ms 延迟。P99 保持在 2ms 以下。" } },
          { "@type": "Question", "name": "能否与 Cursor 内置的 API 密钥一起使用？", "acceptedAnswer": { "@type": "Answer", "text": "可以。只需更改 base URL 设置。继续使用你现有的 API 密钥。" } },
          { "@type": "Question", "name": "这适用于 Cursor Tab 吗？", "acceptedAnswer": { "@type": "Answer", "text": "适用。所有经过 API 的 Cursor AI 功能都会受到保护。" } }
        ]
      }</script>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .seo-page { padding: 8rem 0 4rem; }
  .seo-page .container { max-width: 820px; margin: 0 auto; }

  .seo-header { margin-bottom: 3rem; }

  .seo-badge {
    display: inline-block;
    padding: 0.35rem 0.85rem;
    border-radius: 20px;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--primary);
    background: rgba(134, 239, 172, 0.08);
    border: 1px solid rgba(134, 239, 172, 0.15);
    margin-bottom: 1.25rem;
  }

  .seo-page h1 {
    font-size: 2.75rem;
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 1.15;
    margin-bottom: 1.25rem;
  }

  .seo-subtitle {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.8;
  }

  .seo-tldr {
    padding: 1.25rem 1.5rem;
    margin-bottom: 2.5rem;
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text-primary);
  }

  .seo-tldr strong { color: var(--primary); }
  .seo-tldr code {
    background: var(--bg-deep);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.85rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .seo-page h2 {
    font-size: 1.8rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    margin: 3rem 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--border);
  }

  .seo-page h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 2rem 0 0.75rem;
  }

  .seo-page p {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 1.25rem;
  }

  .seo-page ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .seo-page ul li {
    padding: 0.5rem 0 0.5rem 1.5rem;
    position: relative;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .seo-page ul li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.9rem;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--primary);
    opacity: 0.5;
  }

  .seo-page a { color: var(--primary); text-decoration: underline; text-underline-offset: 2px; }
  .seo-page a:hover { color: var(--accent); }

  /* Code blocks */
  .code-block {
    padding: 1.25rem;
    margin-bottom: 1.5rem;
  }

  .code-block-title {
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .code-block pre {
    margin: 0;
    font-size: 0.85rem;
    line-height: 1.7;
    overflow-x: auto;
  }

  .code-block code { color: var(--primary); font-family: 'JetBrains Mono', monospace; }

  /* Flow diagram */
  .code-compare { margin-bottom: 2.5rem; padding: 1.5rem; }
  .code-compare-header { font-weight: 600; margin-bottom: 1rem; color: var(--text-primary); }

  .flow-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .flow-node {
    padding: 0.6rem 1.25rem;
    border-radius: 10px;
    font-size: 0.85rem;
    font-weight: 600;
    background: rgba(134, 239, 172, 0.06);
    border: 1px solid var(--border);
    color: var(--primary);
  }

  .flow-accent { color: var(--accent); background: rgba(167, 139, 250, 0.06); }
  .flow-danger { color: var(--danger); background: rgba(248, 113, 113, 0.06); }

  .flow-arrow { font-size: 1.25rem; color: var(--text-muted); }

  .flow-note {
    text-align: center;
    font-size: 0.82rem;
    color: var(--text-muted);
    font-style: italic;
  }

  /* Entity grid */
  .entity-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  .entity-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .entity-chip {
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    white-space: nowrap;
  }

  .chip-secret { background: rgba(248, 113, 113, 0.12); color: #f87171; }
  .chip-phone { background: rgba(251, 191, 36, 0.12); color: #fbbf24; }
  .chip-email { background: rgba(96, 165, 250, 0.12); color: #60a5fa; }
  .chip-id { background: rgba(134, 239, 172, 0.12); color: #86efac; }
  .chip-bank { background: rgba(244, 114, 182, 0.12); color: #f472b6; }
  .chip-name { background: rgba(167, 139, 250, 0.12); color: #a78bfa; }
  .chip-loc { background: rgba(252, 165, 165, 0.12); color: #fca5a5; }
  .chip-org { background: rgba(148, 163, 184, 0.12); color: #94a3b8; }

  /* FAQ */
  .seo-faq { padding: 2rem; margin-bottom: 3rem; }
  .seo-faq h2 { border: none; margin-top: 0; }
  .faq-item { padding: 1rem 0; border-bottom: 1px solid var(--border); }
  .faq-item:last-child { border: none; }
  .faq-item h3 { margin: 0 0 0.5rem; font-size: 1.05rem; color: var(--text-primary); }
  .faq-item p { margin: 0; font-size: 0.92rem; }

  /* CTA */
  .seo-cta {
    text-align: center;
    padding: 3rem 2rem;
    background: var(--glass-bg);
    border: 1px solid var(--border);
    border-radius: 20px;
    margin-top: 3rem;
  }
  .seo-cta h2 { margin-top: 0; border: none; }
  .seo-cta p { color: var(--text-secondary); margin-bottom: 1.5rem; }
  .seo-cta-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

  /* Responsive */
  @media (max-width: 768px) {
    .seo-page { padding: 6rem 0 3rem; }
    .seo-page h1 { font-size: 2rem; }
    .seo-page h2 { font-size: 1.4rem; }
    .entity-grid { grid-template-columns: 1fr; }
    .flow-diagram { flex-direction: column; gap: 0.5rem; }
    .code-block pre { font-size: 0.75rem; }
  }

  @media (max-width: 639px) {
    .seo-page h1 { font-size: 1.75rem; }
    .seo-cta-buttons { flex-direction: column; }
    .seo-cta-buttons .btn { width: 100%; }
    .seo-tldr { font-size: 0.88rem; }
  }
</style>
