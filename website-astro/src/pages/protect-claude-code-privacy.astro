---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="Claude Code 隐私保护：阻止源代码与密钥泄漏"
  description="了解 Claude Code 如何将你的代码发送到 Anthropic 的 API，以及如何保护它。30 秒搭建本地隐私代理，在数据离开你的电脑前自动脱敏 API 密钥、数据库凭证和敏感数据。"
  keywords="Claude Code隐私,保护代码不被AI读取,Claude代码安全,Anthropic API安全,AI编程工具数据防护"
  canonicalURL="https://privacygw.pages.dev/protect-claude-code-privacy"
  ogType="article">
  <Nav />

  <section class="seo-page">
    <div class="container">
      <div class="seo-header">
        <div class="seo-badge">Claude Code 隐私指南</div>
        <h1>Claude Code 隐私保护：阻止源代码和密钥泄漏</h1>
        <p class="seo-subtitle">
          Claude Code 是一个强大的 AI 编程助手，运行在你的终端中。但你输入的每一个提示词——
          你引用的每个文件、环境中的每个密钥——都会被发送到 Anthropic 的 API。
          以下是如何让你的敏感数据保持可控。
        </p>
      </div>

      <div class="seo-tldr glass">
        <strong>TL;DR：</strong>Claude Code 将你的终端上下文——包括环境变量、代码片段和文件内容——发送到 Anthropic 的 API。使用 AI Privacy Gateway 作为本地代理，在数据到达 Anthropic 之前自动脱敏 API 密钥、密码、PII 和其他敏感数据。只需修改一个环境变量。30 秒搞定。
      </div>

      <h2>Claude Code 会发送哪些数据？</h2>
      <p>
        当你使用 Claude Code 时，CLI 工具会向 Anthropic 的 API 发送多种类型的数据：
      </p>
      <ul>
        <li><strong>代码和文件内容</strong>来自你当前的项目</li>
        <li><strong>环境变量</strong>可能包含 API 密钥和机密信息</li>
        <li><strong>终端输出</strong>可能包含敏感的命令执行结果</li>
        <li><strong>Git 历史</strong>包括可能含有凭证引用的提交消息</li>
        <li><strong>配置文件</strong>（.env、配置文件）在提示词中被引用</li>
        <li><strong>内部业务逻辑</strong>和专有算法</li>
      </ul>
      <p>
        Anthropic 有严格的数据处理政策，但最安全的方法是确保敏感数据永远不会以明文形式离开你的机器。本地隐私代理是实现这一目标的最有效方式。
      </p>

      <h2>AI Privacy Gateway 如何保护 Claude Code</h2>
      <p>
        AI Privacy Gateway 位于 Claude Code 和 Anthropic 的 API 之间。它扫描每一个外发请求中的敏感模式——API 密钥、密码、手机号、邮箱、信用卡——并在转发给 Anthropic 之前将其替换为安全占位符。
      </p>

      <div class="diagram-flow glass">
        <div class="diagram-row">
          <div class="diagram-node">Claude Code</div>
          <div class="diagram-arrow">&rarr;</div>
          <div class="diagram-node diagram-accent">隐私网关</div>
          <div class="diagram-arrow">&rarr;</div>
          <div class="diagram-node diagram-alt">Anthropic API</div>
        </div>
        <div class="diagram-note">API 密钥、机密信息、PII 在此处脱敏 → 永远不会以明文形式到达 Anthropic</div>
      </div>

      <h2>设置指南：30 秒保护 Claude Code</h2>

      <h3>第 1 步：安装网关</h3>

      <div class="code-block glass">
        <div class="code-block-title">方式 A：Docker</div>
        <pre><code>docker run -d \
  --name ai-privacy-gw \
  -p 9999:9999 \
  -v ./vault_data:/app/vault_data \
  ghcr.io/gunxueqiu6/ai-privacy-gateway:lite</code></pre>
      </div>

      <div class="code-block glass">
        <div class="code-block-title">方式 B：Python (pip)</div>
        <pre><code>git clone https://github.com/gunxueqiu6/ai-privacy-gateway
cd ai-privacy-gateway
pip install -r requirements.txt
python main.py</code></pre>
      </div>

      <h3>第 2 步：配置 Claude Code</h3>
      <p>在启动 Claude Code 之前设置 <code>ANTHROPIC_BASE_URL</code> 环境变量：</p>

      <div class="code-block glass">
        <div class="code-block-title">macOS / Linux</div>
        <pre><code>export ANTHROPIC_BASE_URL=http://localhost:9999/v1
claude</code></pre>
      </div>

      <div class="code-block glass">
        <div class="code-block-title">Windows (PowerShell)</div>
        <pre><code>$env:ANTHROPIC_BASE_URL = "http://localhost:9999/v1"
claude</code></pre>
      </div>

      <p>你现有的 <code>ANTHROPIC_API_KEY</code> 仍然有效——网关会自动转发它。</p>

      <h3>第 3 步：验证是否生效</h3>
      <p>
        向 Claude Code 提问一个涉及包含 API 密钥或手机号的文件的问题。
        打开网关的管理面板 <a href="http://localhost:9999/admin">http://localhost:9999/admin</a>
        即可实时查看被脱敏的实体。
      </p>

      <h2>Claude Code 用户正在脱敏的内容</h2>
      <div class="entity-grid">
        <div class="entity-item"><span class="entity-chip chip-secret">API 密钥</span> OpenAPI, AWS, GitHub 令牌在环境和代码中</div>
        <div class="entity-item"><span class="entity-chip chip-phone">手机号</span> 配置文件中的联系号码</div>
        <div class="entity-item"><span class="entity-chip chip-email">邮箱</span> 开发者邮箱、测试数据中的用户邮箱</div>
        <div class="entity-item"><span class="entity-chip chip-id">身份证</span> 数据样本中的用户身份证号</div>
        <div class="entity-item"><span class="entity-chip chip-bank">银行卡</span> 支付处理代码中的测试卡号</div>
        <div class="entity-item"><span class="entity-chip chip-name">姓名</span> 代码中的员工姓名、客户姓名</div>
        <div class="entity-item"><span class="entity-chip chip-loc">地址</span> 商业地址、客户位置</div>
        <div class="entity-item"><span class="entity-chip chip-org">机密</span> 数据库 URL、密码、连接字符串</div>
      </div>

      <!-- FAQ -->
      <div class="seo-faq glass" id="faq">
        <h2>常见问题</h2>

        <div class="faq-item">
          <h3>网关会增加 Claude Code 的延迟吗？</h3>
          <p>不会。网关平均延迟增加不超过 1ms。P99 延迟保持在 2ms 以下。Claude Code 的终端体验完全不受影响。</p>
        </div>

        <div class="faq-item">
          <h3>这会破坏 Claude Code 的上下文功能吗？</h3>
          <p>不会。网关只脱敏敏感数据模式。所有代码上下文、文件引用和对话历史都能正常通过——只是敏感数据被替换为安全占位符。</p>
        </div>

        <div class="faq-item">
          <h3>我可以在直接使用 Claude API 时使用它吗？</h3>
          <p>可以。将任何兼容 OpenAI 或 Anthropic 的客户端指向 <code>http://localhost:9999/v1</code>。网关适用于任何支持聊天补全格式的 API。</p>
        </div>

        <div class="faq-item">
          <h3>它支持流式传输（SSE）吗？</h3>
          <p>支持。网关完全支持 SSE 流式传输，具有实时脱敏和恢复响应数据的功能。</p>
        </div>

        <div class="faq-item">
          <h3>商业使用免费吗？</h3>
          <p>免费。AI Privacy Gateway 使用 MIT 许可证。个人和商业使用均免费。</p>
        </div>
      </div>

      <!-- CTA -->
      <div class="seo-cta">
        <h2>保护你的 Claude Code 会话</h2>
        <p>30 秒部署 AI Privacy Gateway。你的数据留在你的机器上。</p>
        <div class="seo-cta-buttons">
          <a href="/download" class="btn btn-primary btn-lg">免费开始使用</a>
          <a href="/demo" class="btn btn-ghost btn-lg">试用演示</a>
        </div>
      </div>

      <!-- FAQ Schema -->
      <script type="application/ld+json">{
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          { "@type": "Question", "name": "网关会增加 Claude Code 的延迟吗？", "acceptedAnswer": { "@type": "Answer", "text": "不会。网关平均延迟增加不超过 1ms。" } },
          { "@type": "Question", "name": "这会破坏 Claude Code 的上下文功能吗？", "acceptedAnswer": { "@type": "Answer", "text": "不会。网关只脱敏敏感数据模式，保留所有上下文。" } },
          { "@type": "Question", "name": "它支持流式传输吗？", "acceptedAnswer": { "@type": "Answer", "text": "支持。完全支持 SSE 流式传输，具有实时脱敏和恢复功能。" } }
        ]
      }</script>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .seo-page { padding: 8rem 0 4rem; }
  .seo-page .container { max-width: 820px; margin: 0 auto; }

  .seo-header { margin-bottom: 3rem; }
  .seo-badge {
    display: inline-block;
    padding: 0.35rem 0.85rem;
    border-radius: 20px;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--primary);
    background: rgba(134, 239, 172, 0.08);
    border: 1px solid rgba(134, 239, 172, 0.15);
    margin-bottom: 1.25rem;
  }

  .seo-page h1 {
    font-size: 2.75rem;
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 1.15;
    margin-bottom: 1.25rem;
  }

  .seo-subtitle {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.8;
  }

  .seo-tldr {
    padding: 1.25rem 1.5rem;
    margin-bottom: 2.5rem;
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text-primary);
  }

  .seo-tldr strong { color: var(--primary); }
  .seo-tldr code {
    background: var(--bg-deep);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-size: 0.85rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .seo-page h2 {
    font-size: 1.8rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    margin: 3rem 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--border);
  }

  .seo-page h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 2rem 0 0.75rem;
  }

  .seo-page p {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 1.25rem;
  }

  .seo-page ul {
    list-style: none;
    padding: 0;
    margin-bottom: 1.5rem;
  }

  .seo-page ul li {
    padding: 0.5rem 0 0.5rem 1.5rem;
    position: relative;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .seo-page ul li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.9rem;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--primary);
    opacity: 0.5;
  }

  .seo-page a { color: var(--primary); text-decoration: underline; text-underline-offset: 2px; }
  .seo-page a:hover { color: var(--accent); }

  .code-block { padding: 1.25rem; margin-bottom: 1.5rem; }
  .code-block-title {
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .code-block pre { margin: 0; font-size: 0.85rem; line-height: 1.7; overflow-x: auto; }
  .code-block code { color: var(--primary); font-family: 'JetBrains Mono', monospace; }

  .diagram-flow { padding: 1.5rem; margin-bottom: 2.5rem; }
  .diagram-row {
    display: flex; align-items: center; justify-content: center;
    gap: 0.75rem; margin-bottom: 0.75rem;
  }
  .diagram-node {
    padding: 0.6rem 1.25rem; border-radius: 10px;
    font-size: 0.85rem; font-weight: 600;
    background: rgba(134, 239, 172, 0.06);
    border: 1px solid var(--border); color: var(--primary);
  }
  .diagram-accent { color: var(--accent); background: rgba(167, 139, 250, 0.06); }
  .diagram-alt { color: var(--warning); background: rgba(251, 191, 36, 0.06); }
  .diagram-arrow { font-size: 1.25rem; color: var(--text-muted); }
  .diagram-note { text-align: center; font-size: 0.82rem; color: var(--text-muted); font-style: italic; }

  .entity-grid {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 0.75rem; margin-bottom: 2rem;
  }
  .entity-item {
    display: flex; align-items: center;
    gap: 0.75rem; font-size: 0.88rem;
    color: var(--text-secondary); line-height: 1.5;
  }
  .entity-chip {
    font-size: 0.7rem; font-weight: 600;
    padding: 0.2rem 0.5rem; border-radius: 4px; white-space: nowrap;
  }
  .chip-secret { background: rgba(248, 113, 113, 0.12); color: #f87171; }
  .chip-phone { background: rgba(251, 191, 36, 0.12); color: #fbbf24; }
  .chip-email { background: rgba(96, 165, 250, 0.12); color: #60a5fa; }
  .chip-id { background: rgba(134, 239, 172, 0.12); color: #86efac; }
  .chip-bank { background: rgba(244, 114, 182, 0.12); color: #f472b6; }
  .chip-name { background: rgba(167, 139, 250, 0.12); color: #a78bfa; }
  .chip-loc { background: rgba(252, 165, 165, 0.12); color: #fca5a5; }
  .chip-org { background: rgba(148, 163, 184, 0.12); color: #94a3b8; }

  .seo-faq { padding: 2rem; margin-bottom: 3rem; }
  .seo-faq h2 { border: none; margin-top: 0; }
  .faq-item { padding: 1rem 0; border-bottom: 1px solid var(--border); }
  .faq-item:last-child { border: none; }
  .faq-item h3 { margin: 0 0 0.5rem; font-size: 1.05rem; color: var(--text-primary); }
  .faq-item p { margin: 0; font-size: 0.92rem; }

  .seo-cta {
    text-align: center; padding: 3rem 2rem;
    background: var(--glass-bg); border: 1px solid var(--border);
    border-radius: 20px; margin-top: 3rem;
  }
  .seo-cta h2 { margin-top: 0; border: none; }
  .seo-cta p { color: var(--text-secondary); margin-bottom: 1.5rem; }
  .seo-cta-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

  @media (max-width: 768px) {
    .seo-page { padding: 6rem 0 3rem; }
    .seo-page h1 { font-size: 2rem; }
    .seo-page h2 { font-size: 1.4rem; }
    .entity-grid { grid-template-columns: 1fr; }
    .code-block pre { font-size: 0.75rem; }
    .diagram-row { flex-direction: column; gap: 0.5rem; }
  }
  @media (max-width: 639px) {
    .seo-page h1 { font-size: 1.75rem; }
    .seo-cta-buttons { flex-direction: column; }
    .seo-cta-buttons .btn { width: 100%; }
  }
</style>
